<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="th.ac.sk.timetableapp">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".MasterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".display.SplashScreenActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".modify.ModifyTeacherLocationChooserActivity" />
        <activity android:name=".modify.ModifyTeacherLocationEditorActivity" />
        <activity android:name=".display.DayDisplayActivity" />
        <activity android:name=".display.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".display.PeriodDisplayActivity" />
        <activity android:name=".display.TeacherLocationDisplayActivity" />
        <activity android:name=".modify.ModifyClassroomActivity" />
        <activity android:name=".settings.SettingsActivity" />
    </application>

</manifest>